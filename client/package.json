{
  "name": "digital-quiz-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke": "esbuild scripts/smoke.ts --bundle --platform=node --format=esm --outfile=node_modules/.smoke.mjs --log-level=warning && node node_modules/.smoke.mjs"
  },
  "dependencies": {
    "firebase": "^12.18.0",
    "spacetimedb": "^2.8.3",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "~5.6.2",
    "vite": "^6.0.0"
  }
}
